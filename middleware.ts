import { NextRequest, NextResponse } from "next/server";`n`nexport function middleware(request: NextRequest) {`n  // Protect /app/* routes - require authentication`n  if (request.nextUrl.pathname.startsWith("/app")) {`n    // Add authentication check here`n    // For now, just pass through`n  }`n  return NextResponse.next();`n}`n`nexport const config = {`n  matcher: ["/app/:path*"]`n};
